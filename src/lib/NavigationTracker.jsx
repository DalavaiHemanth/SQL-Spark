import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';

export default function NavigationTracker() {
    // Navigation tracking — no-op (placeholder for future analytics)
    return null;
}